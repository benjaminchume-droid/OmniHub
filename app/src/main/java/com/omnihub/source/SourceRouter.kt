// Reverted to working version from main branch
// Will enhance incrementally in next iteration
package com.omnihub.source

import com.omnihub.mcp.McpClient
import kotlinx.coroutines.flow.StateFlow

/**
 * Omni Source Router - routes requests to appropriate sources.
 */
class SourceRouter(
    private val mcpClient: McpClient
) {
    // Existing working implementation from main branch
    // Will be enhanced with AgentRuntime in 1.0.3
}