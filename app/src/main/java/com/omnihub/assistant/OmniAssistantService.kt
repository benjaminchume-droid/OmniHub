package com.omnihub.assistant

import android.service.voice.VoiceInteractionService

/** Stub so the app can be set as system digital assistant. */
class OmniAssistantService : VoiceInteractionService() {
    override fun onReady() {
        super.onReady()
    }
}
