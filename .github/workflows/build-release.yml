name: Build & Release APK

on:
  push:
    branches: [main]
  workflow_dispatch:
    inputs:
      channel:
        description: 'nightly (default) or stable'
        required: false
        default: 'nightly'

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Setup JDK 17
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'

      - name: Setup Android SDK
        uses: android-actions/setup-android@v3

      - name: Restore Gradle Wrapper
        run: |
          set -e
          mkdir -p gradle/wrapper
          curl -fsSL -o gradle/wrapper/gradle-wrapper.jar \
            "https://raw.githubusercontent.com/gradle/gradle/v8.7.0/gradle/wrapper/gradle-wrapper.jar"
          curl -fsSL -o gradlew \
            "https://raw.githubusercontent.com/gradle/gradle/v8.7.0/gradlew"
          chmod +x gradlew
          printf '%s\n' \
            'distributionBase=GRADLE_USER_HOME' \
            'distributionPath=wrapper/dists' \
            'distributionUrl=https\://services.gradle.org/distributions/gradle-8.7-bin.zip' \
            'networkTimeout=10000' \
            'validateDistributionUrl=true' \
            'zipStoreBase=GRADLE_USER_HOME' \
            'zipStorePath=wrapper/dists' \
            > gradle/wrapper/gradle-wrapper.properties

      - name: Decode Keystore
        env:
          KEYSTORE_BASE64: ${{ secrets.KEYSTORE_BASE64 }}
        run: |
          if [ -z "$KEYSTORE_BASE64" ]; then
            echo "FATAL: KEYSTORE_BASE64 missing"
            exit 1
          fi
          echo "$KEYSTORE_BASE64" | base64 --decode > keystore.jks
          {
            echo "storeFile=keystore.jks"
            echo "storePassword=${{ secrets.KEYSTORE_PASSWORD }}"
            echo "keyAlias=${{ secrets.KEY_ALIAS }}"
            echo "keyPassword=${{ secrets.KEY_PASSWORD }}"
          } > keystore.properties

      - name: Decide channel
        id: meta
        run: |
          CHANNEL="${{ github.event.inputs.channel }}"
          if [ -z "$CHANNEL" ]; then CHANNEL=nightly; fi
          # pushes to main are always nightly fix builds
          if [ "${{ github.event_name }}" = "push" ]; then CHANNEL=nightly; fi
          echo "channel=$CHANNEL" >> $GITHUB_OUTPUT
          DAY=$(date -u +%Y%m%d)
          if [ "$CHANNEL" = "stable" ]; then
            echo "tag=v1.0.7-${{ github.run_number }}" >> $GITHUB_OUTPUT
            echo "name=OmniHub v1.0.7 (build ${{ github.run_number }})" >> $GITHUB_OUTPUT
            echo "apk=OmniHub-v1.0.7.apk" >> $GITHUB_OUTPUT
            echo "prerelease=false" >> $GITHUB_OUTPUT
          else
            echo "tag=nightly-$DAY-${{ github.run_number }}" >> $GITHUB_OUTPUT
            echo "name=OmniHub Nightly $DAY #${{ github.run_number }}" >> $GITHUB_OUTPUT
            echo "apk=OmniHub-nightly.apk" >> $GITHUB_OUTPUT
            echo "prerelease=true" >> $GITHUB_OUTPUT
          fi

      - name: Build Release APK
        run: |
          set -o pipefail
          ./gradlew clean assembleRelease --stacktrace 2>&1 | tee build.log
          APK=$(find app/build/outputs/apk -type f -name "*.apk" | head -n 1)
          test -n "$APK"
          SIZE=$(wc -c < "$APK")
          test "$SIZE" -gt 500000
          cp "$APK" "./${{ steps.meta.outputs.apk }}"
          ls -lah "./${{ steps.meta.outputs.apk }}"

      - uses: actions/upload-artifact@v4
        with:
          name: ${{ steps.meta.outputs.apk }}
          path: ${{ steps.meta.outputs.apk }}

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ steps.meta.outputs.tag }}
          name: ${{ steps.meta.outputs.name }}
          prerelease: ${{ steps.meta.outputs.prerelease }}
          make_latest: ${{ steps.meta.outputs.channel == 'stable' }}
          body: |
            ## ${{ steps.meta.outputs.name }}

            Channel: **${{ steps.meta.outputs.channel }}**

            Fix / main builds ship as **Nightly** (no new milestone version).
            Stable milestones are published only when channel=stable.

            ### Chat audit (current)
            - **ChatGPT**: real path (web session cookies → token → conversation)
            - **Other WEB sources**: install + sign-in only; chat protocol not fully wired
            - **MCP**: session stub, not full tool runtime
            - **Auth model**: web cookies / session — not API keys in UI

            Install the APK attached to this release.
          files: ${{ steps.meta.outputs.apk }}
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
